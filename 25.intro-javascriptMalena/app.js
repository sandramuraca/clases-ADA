/* Las ordenes en javascript se pueden escribir una etiqueta script en el body o linkear un archivo JS

el archivo de hmtl tiene que tener el mismo nombre que el JS
no poner nombres distintos

*/

//variable: espacio de memoria
// no puedo declarar una variable 2 veces con el mismo nombre, si puedo reasignar un valor a esa variable

let nombre = "Sandra";
